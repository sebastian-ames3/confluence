# Nixpacks configuration for Railway deployment
# Use Python provider with pip install

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg", "chromium", "chromedriver"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install --no-cache-dir -r requirements.txt && echo 'PIP INSTALL COMPLETE'"]

[start]
cmd = "python3 scripts/run_migrations.py --auto && python3 -m uvicorn backend.app:app --host 0.0.0.0 --port $PORT"

# Force rebuild v5
[variables]
NIXPACKS_FORCE_REBUILD = "5"
